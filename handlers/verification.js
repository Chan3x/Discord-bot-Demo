module.exports = verification  = e => {
    console.log(e.d)
    const {user_id, message_id, emoji, channel_id, guild_id} = e.d
    const member = 
    
   // user_id.addRole('verified').catch(console.error)
}